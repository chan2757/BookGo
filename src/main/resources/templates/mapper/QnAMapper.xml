<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bookgo.mapper.QnAMapper">

    <insert id="insertQnA" parameterType="com.bookgo.vo.QnAVO">
        INSERT INTO QNA (QID, TITLE, WRITER, CONTENT, FILENAME, WRITEDATE, READCNT, STATUS)
        VALUES (QNA_SEQ.NEXTVAL, #{title}, #{writer}, #{content}, #{filename}, #{writeDate}, 0, '미답변')
    </insert>

</mapper>
